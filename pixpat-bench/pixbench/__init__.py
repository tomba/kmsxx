"""pixbench: build, measure and compare pixpat native libraries.

Correctness (a digest of every output byte), throughput and shared
library size, on any source tree, built library or commit; every
measurement lands in a JSON run file that `compare`, `report` and
`export` read back. See README.md.
"""
