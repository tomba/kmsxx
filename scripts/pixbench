#!/usr/bin/env python3
"""Launcher for the pixbench package in pixpat-bench/."""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent / 'pixpat-bench'))

from pixbench.cli import main

sys.exit(main())
